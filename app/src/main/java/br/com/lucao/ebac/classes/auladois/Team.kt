package br.com.lucao.ebac.classes.auladois

/**
class Team {
   var player: List<Person>
   var name: String

   fun getAllPlayerWithNameBiggerThan(size: Int): List<Person>{
      return this.player.filter{it ->
         it.name.length > size
      }
   }

   fun printAllPlayersEmailAndPhone(): Unit {
      this.player.forEach{player ->
         println(player.getEmailAndPhone())
      }
   }
}*/